"use strict";

module.exports = {
	getActiveEmployees: getActiveEmployees,
	needsToAddTime: needsToAddTime
};

/**
 * Uses casper to check all active employees
 * @return {Array of strings} - List of active employees' usernames
 */
function getActiveEmployees() {

}

/**
 * Uses casper to check if the user has time
 * @param  {string} name - Username of employee (jsallans)
 * @return {bool} - True if the user needs to add time
 */
function needsToAddTime(name) {

}